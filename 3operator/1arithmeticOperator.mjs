console.log(1 + 2); // addition
console.log(5 - 3);                 
console.log(4 * 2); // multiplication
console.log(10 / 2);                    
console.log(10 % 3); // modulus operator gives the remainder


//+ operator can also be used for string concatenation
console.log("3 " + "1"); // concatenation of two strings
console.log("sushant " + "bro"); // concatenation of two strings

//the color of the output will be different for numbers and strings in the
//  terminal, i.e white strings and yellow for numbers.

console.log("3" + 1); // concatenation of string and number, the number will be converted to string
console.log(1+1+"1"+1); // the output will be 211 because the first two numbers will be added and then
//  the result will be concatenated with the string "1" and then the
//  final result will be concatenated with the number 1 which will be converted to string.